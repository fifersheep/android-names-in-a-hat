<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_sort_names"
        android:icon="@drawable/ic_action_sort_by_size"
        android:title="Sort Names"
        app:showAsAction="always|withText"/>

    <item
        android:id="@+id/action_add_name"
        android:icon="@drawable/ic_action_add_person"
        android:title="New Name"
        app:showAsAction="always|withText" />

    <item
        android:id="@+id/action_class_options"
        android:icon="@drawable/ic_action_group"
        android:title="Class Options"
        app:showAsAction="always|withText">

        <menu>
            <item
                android:id="@+id/action_add_class"
                android:icon="@drawable/ic_action_add_group"
                android:title="New Class"
                app:showAsAction="always|withText" />

            <item
                android:id="@+id/action_edit_class_name"
                android:icon="@drawable/ic_action_edit"
                android:title="Edit Class Name"
                app:showAsAction="always|withText" />

            <item
                android:id="@+id/action_delete_class"
                android:icon="@drawable/ic_action_discard"
                android:title="Delete Class"
                app:showAsAction="always|withText" />
        </menu>
    </item>
</menu> 